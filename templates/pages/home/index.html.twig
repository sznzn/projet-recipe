{% extends 'base.html.twig' %}

{% block title %}Bienvenu chez mon page Symfony !{% endblock %}

{% block body %}
    
<div class="container mt-4">
    <div class="jumbotron">
        <h1 class="display-4">Bienvenu sur EasyRecipe</h1> 
        <p class="lead">EasyRecip est une application qui va te permettre de créer des recettes à base d'ingrédients que tu auras toi-même créer. Tu pourras partager tes recettes à la communauté du site, ou bien les garder en privées.</p> 
        <hr class="my-4">
        {% if app.user %}
        <a class="btn btn-primary btn-lgz" href="{{path('recipe.index')}}" role="button">Votre Recette</a>
            {% else %}
        <p>Pour commencer, rendez-vous sur la page d'inscription pour utiliser l'application.</p>
        <a class="btn btn-primary btn-lgz" href="{{path('security.registration')}}" role="button">Inscription</a>
        {% endif %}

</div>
    <div class="recipes">
        <h2 class="my-4 text-center">Recettes de la communauté</h2>
        <div class="d-flex justify-content-center flex-wrap">
                {% for recipe in recipes %}
                <div class="card border-primary mx-3" style="max-width: 20rem;">
                    <div class="card-header">Recette n° {{recipe.id}}</div>
                        <div class="card-body">
                            <h4 class="card-title">{{recipe.name}}</h4>
                            <p class="card-text">{{recipe.description|slice(0,100) ~ '...'}}</p>
                        </div>
                </div>
                {% endfor %}
        </div>

    </div>
</div>


{% endblock %}
